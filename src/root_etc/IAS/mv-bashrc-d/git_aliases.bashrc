#!/bin/bash

# Git aliases
alias git-treel='/usr/bin/git log --all --graph'
alias git-tree='/usr/bin/git log --all --graph --oneline'

# This is all caps because you need to type
# "cd $(GR)" to use it
alias GR="git rev-parse --show-toplevel"
